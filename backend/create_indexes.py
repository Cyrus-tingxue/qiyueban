from app.database import engine; from sqlalchemy import text; with engine.connect() as con: con.execute(text("CREATE INDEX IF NOT EXISTS ix_posts_created_at ON posts(created_at); CREATE INDEX IF NOT EXISTS ix_posts_author_id ON posts(author_id); CREATE INDEX IF NOT EXISTS ix_replies_created_at ON replies(created_at); CREATE INDEX IF NOT EXISTS ix_replies_author_id ON replies(author_id);")); con.commit()
